'use strict';

/**
 *  contacts-marketing controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contacts-marketing.contacts-marketing');
