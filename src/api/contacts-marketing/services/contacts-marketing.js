'use strict';

/**
 * contacts-marketing service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contacts-marketing.contacts-marketing');
