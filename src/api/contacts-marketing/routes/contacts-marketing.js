'use strict';

/**
 * contacts-marketing router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contacts-marketing.contacts-marketing');
